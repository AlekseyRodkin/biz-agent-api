SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-chat
EMBEDDING_MODEL=intfloat/multilingual-e5-small
USER_ID=alexey
# Auth tokens (supports rotation without downtime)
# Either use ADMIN_TOKEN (simple) or ADMIN_TOKEN_CURRENT + ADMIN_TOKEN_NEXT (rotation)
ADMIN_TOKEN_CURRENT=change_me_to_secure_token
# ADMIN_TOKEN_NEXT=  # Set during rotation, then swap to CURRENT
